<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yudev.tesandroiddevkaspin">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TesAndroidDevKaspin">
        <activity android:name=".view.activity.SuccesActivity"></activity>
        <activity android:name=".view.activity.ChekoutActivity" />
        <activity android:name=".view.activity.TambahBarangActivity" />
        <activity android:name=".view.activity.DaftarTransaksiActivity" />
        <activity android:name=".view.activity.DaftarBarangActivity" />
        <activity android:name=".view.activity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.activity.MainActivity" />
    </application>

</manifest>